// import SuccessCode from '@enums/success-code.enum';

// const keys = Object.keys(SuccessCode);

// const messages = keys.map((key: string) => {
//   return {
//     [key]: {
//       type: 'string',
//       default: SuccessCode[key],
//     },
//   };
// });

// export default messages.reduce(
//   (prevValue: object, currValue: object) => ({
//     ...prevValue,
//     ...currValue,
//   }),
//   Object.create(null),
// );
